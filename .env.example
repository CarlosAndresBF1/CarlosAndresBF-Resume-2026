# ── SMTP Configuration for Contact Form ──
# Provider examples:
#   Gmail:     smtp.gmail.com  / port 587 / secure false (uses STARTTLS)
#   Outlook:   smtp.office365.com / port 587 / secure false
#   SendGrid:  smtp.sendgrid.net / port 587 / secure false
#   Zoho:      smtp.zoho.com  / port 465 / secure true
# myaccount.google.com/apppasswords

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Recipient email for contact form submissions
CONTACT_TO_EMAIL=carlosandresbeltran89@gmail.com

# "From" address (defaults to SMTP_USER if omitted)
# CONTACT_FROM_EMAIL=noreply@yourdomain.com
